<div class="container px-5 px-sm-0">
  <div class="row">
   <div class="col-md-3">
     <% if @book.errors.any? %>
      <%= @book.errors.count %>errors prohibited this obj from being saved:
      <ul>
        <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>
<%= render 'users/list', user: @user, book: @book_new %>
</div>

<div class="col-md-8 offset-md-1">
<h2>Book detail</h2>
   <table class="table">

     <tbody>

        <tr>
          <td><%= image_tag @user.get_profile_image(100,100) %><%= link_to @user.name, user_path(current_user.id) %></td>
          <td><%= link_to @book.title, book_path(current_user.id) %></td>
          <td><%= @book.body %></td>
          <td><%= link_to 'Edit', edit_book_path(@book.id), class: "btn btn-sm btn-success" %></td>
          <td><%= link_to 'Destroy', book_path(@book.id), class: "btn btn-sm btn-danger", method: :delete, "data-confirm" => "Are you sure？" %></td>

        </tr>

     </tbody>
   </table>
</div>
</div>
</div>